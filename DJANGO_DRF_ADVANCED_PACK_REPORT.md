# DJANGO_DRF_ADVANCED_PACK_REPORT.md

## Pack

`v1.7 — Advanced Django/DRF Pack`

## Mục tiêu

Bổ sung nhóm pattern Django/DRF nâng cao để thư viện không chỉ tập trung FastAPI mà cũng đủ dùng cho dự án Django production.

## Đã thêm

### Decision cards

- Xây API bằng Django/DRF
- Deploy Django production

### Libraries

- django-environ
- SimpleJWT
- django-filter
- pytest-django

### Patterns mới

1. Django Settings Split
2. Django Custom User Model
3. Django Admin Hardening
4. DRF Serializer Advanced
5. DRF ViewSet Advanced
6. DRF Permissions Policy
7. DRF Filtering / Search / Ordering
8. DRF Throttling / Rate Limit
9. DRF SimpleJWT Authentication
10. Django Celery Integration
11. Django Management Command
12. Django File Upload Security
13. Django Testing Fixtures
14. Django Deployment Checklist

## Trạng thái

- App vẫn static-only.
- Không thêm backend runtime.
- Không thêm database runtime.
- Không thêm Gemini/OpenAI generator.
- Nội dung là static TypeScript data.
- Search/filter tiếp tục dùng dữ liệu static.

## Kiểm tra gợi ý

Search các từ khóa:

- `django settings`
- `custom user`
- `drf serializer`
- `viewset`
- `simplejwt`
- `django celery`
- `django upload`
- `pytest django`
- `django deployment`
